[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hstrat-rs"
version = "0.1.0"
description = "Hereditary stratigraphy for phylogenetic inference — Rust backend"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3"]
